"use strict";

import * as argon2 from "argon2";

export class pwhaas {
    static async hash(plain: string): Promise<string> {
        throw new Error("Not implemented.");
    }
    static async verify(plain: string): Promise<string> {
        throw new Error("Not implemented.");
    }
}